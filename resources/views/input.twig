{{ add_asset("streams.css", "anomaly.field_type.boolean::css/bootstrap-toggle.css") }}
{{ add_asset("streams.js", "anomaly.field_type.boolean::js/bootstrap-toggle.min.js") }}

<input
        type="checkbox"
        class="form-control"
        name="{{ field_type.getFieldName() }}"
        data-toggle="toggle"
        data-on="{{ field_type.config.on }}"
        data-off="{{ field_type.config.off }}"
        data-size="{{ field_type.config.size }}"
        data-onstyle="{{ field_type.config.on_style }}"
        data-offstyle="{{ field_type.config.off_style }}"
        {{ field_type.value ? 'checked' }}
        >